{
  "name": "pass-date-resolver",
  "version": "1.0.0",
  "type": "module",
  "description": "Resolve timestamps for entities in PASS and append local duplicates database (use in conjunction with the pass dupe checker)",
  "main": "./index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^7.5.0",
    "node-fetch": "^3.2.0"
  }
}
